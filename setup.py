from setuptools import setup, find_packages

setup(
    name="petvision-backend",
    version="1.0.0",
    description="Backend image classification system for PetVision shelter intake app.",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.10",
)
